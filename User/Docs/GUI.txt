【方案】：
    出于u8g2库有些庞大并且有许多内容都用不到的原因，现在考虑自己搭建一个适配的GUI库。


   【API函数】
   我的LCD是单色屏，规格为128*64，8行为一页，共128列和8页。每列中每页8个像素点，即一个字节，对于这列这页写入一个字节，
字节的低位对应的是实际低位像素点（比如对于一个字节bit0~7，在第一列的第一页中，bit0对应的就是纵坐标0，bit7对应的就是纵坐标7）。现在我打算使用脏矩形算法，
构建一个GUI系统，提供各种绘制函数供调用，可以在while循环里，通过调用gui_handler来刷新屏幕。下面是我刚开始构建的gui库，从中可以看出，绘制操作均在缓冲区里进行，
刷新则是统一进行的。使用的是C89语法，for循环里不能定义变量，这一点需要格外注意，此外abs、MIN等一些标准库函数也不能使用。
    这是我的绘制横线段函数：